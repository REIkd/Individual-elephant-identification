# 远程 AI 服务器部署（外网端口 9998）

适用于：云 GPU 机器、公网 IP + 指定开放端口（例如 9998），Pi 或手机通过公网访问。

---

## 架构

```
[树莓派] 摄像头 → 上传 JPEG
       ↓
[AI 服务器 120.196.88.140:9998]  cloud_server.py
       ↓
JSON 框 + 名字 → Pi 本地显示
```

**注意**：公网部署务必设置 **API Key**，不要用空密钥。

---

## 第一步：SSH 登录服务器

你提供的连接信息（端口以 **12222** 为准；若 `-p 1222` 连不上请改 12222）：

```bash
ssh root@120.196.88.140 -p 12222
```

---

## 第二步：上传项目到服务器

在 **Windows 本机** 项目目录打开 PowerShell，上传云端所需文件：

```powershell
cd "D:\Project\Individual elephant identification"

scp -P 12222 `
  cloud_server.py cloud_inference.py video_tracker_yolo.py `
  predict.py elephant_net.py class_names.json `
  best_elephant_model.pth requirements-cloud.txt `
  start_cloud_server_linux.sh `
  root@120.196.88.140:/root/elephant_cloud/
```

首次需在服务器创建目录：

```bash
ssh root@120.196.88.140 -p 12222 "mkdir -p /root/elephant_cloud"
```

模型较大时上传较慢，请耐心等待。

---

## 第三步：在服务器安装依赖

SSH 登录后：

```bash
cd /root/elephant_cloud
chmod +x start_cloud_server_linux.sh

python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -r requirements-cloud.txt

# 有 NVIDIA GPU 时，按 https://pytorch.org 安装对应 CUDA 版 torch（可选，CPU 也能跑但较慢）
```

---

## 第四步：选端口（9998 与 8000 的关系）

| 情况 | 做法 |
|------|------|
| 平台「外网 9998 → 内网 9998」 | 服务监听 **9998**（推荐，最简单） |
| 平台「外网 9998 → 内网 8000」 | 服务监听 **8000**，外网仍用 9998 访问 |
| 服务器上 8000 已被别的程序占用 | 改用 **9998** 或其他未占用端口，并与平台映射一致 |

本项目的启动脚本默认 **9998**：

```bash
export CLOUD_API_KEY="请改成足够长的随机字符串"
export CLOUD_PORT=9998
./start_cloud_server_linux.sh
```

或一行启动：

```bash
CLOUD_API_KEY="your-secret-key" CLOUD_PORT=9998 ./start_cloud_server_linux.sh
```

**后台常驻（断开 SSH 仍运行）**：

```bash
cd /root/elephant_cloud
source .venv/bin/activate
nohup env CLOUD_API_KEY="your-secret-key" CLOUD_PORT=9998 \
  ./start_cloud_server_linux.sh > cloud.log 2>&1 &
tail -f cloud.log
```

---

## 第五步：在服务器上自测

```bash
curl http://127.0.0.1:9998/health
```

应返回：`{"status":"ok","service":"elephant-cloud",...}`

---

## 第六步：在本机 / Pi 测外网

**Windows（本机）**：

```powershell
curl.exe http://120.196.88.140:9998/health
```

**树莓派** `pi_cloud_config.sh`：

```bash
export ELEPHANT_SERVER="http://120.196.88.140:9998"
export CLOUD_API_KEY="与服务器 CLOUD_API_KEY 完全一致"
```

然后：

```bash
./run_pi_cloud_client.sh
```

---

## 常见问题

### 1. 外网 curl 不通，服务器内网 curl 通

- 检查云平台「安全组 / 防火墙」是否放行 **9998**
- 确认服务 `--host 0.0.0.0`，不是 `127.0.0.1`

### 2. 8000 被占用

与 Pi 无关。Pi 只连 **外网开放的 9998**。服务器内部用 9998 启动即可，不必占用 8000。

### 3. 401 / 无效 API Key

Pi 的 `CLOUD_API_KEY` 必须与服务器 `CLOUD_API_KEY` **完全一致**（区分大小写）。

### 4. 首次推理很慢

首次会下载 `yolov8m.pt`，属正常现象。

---

## 安全提醒

- 勿将 root 密码、API Key 提交到 Git 或发给他人
- 公网务必使用强随机 `CLOUD_API_KEY`
- 若仅 Pi 在家用，可优先考虑「Pi + 家里 PC 局域网」方案，不必暴露公网
