# 红外相机心跳接口 · 代码实现总结

> **项目：** 大象个体识别云端平台  
> **服务器：** `120.196.88.140:9998`  
> **项目路径：** `/root/elephant_cloud`  
> **文档版本：** 2026-07-20  
> **对接依据：** UOVision 广东动物所对接文档 · 章节 1.4

---

## 1. 功能概述

相机定时向识别服务器 POST JSON 状态（IMEI、GPS、电量、信号、SD 卡等）。平台：

1. 接收并校验 `cameraCode`（IMEI）
2. 写入内存索引 + 磁盘 `latest.json`
3. 在 `/watch/ir` 总览页显示 **在线/离线**
4. 提供查询 API 供运维与网页轮询

**无需 API Key**（与 `/servlet/original` 上传接口相同）。

---

## 2. 涉及文件（需上传服务器）

| 文件 | 改动说明 |
|------|----------|
| `uovision_camera.py` | 心跳核心逻辑：解析 JSON、落盘、在线判定 |
| `cloud_server.py` | 注册 HTTP 路由、网页总览展示 |
| `uovision_registry.py` | 相机列表 API 合并心跳状态 |
| `红外相机对接接口清单.md` | 完整接口清单（含 1.4 心跳） |
| `UOVision红外相机对接说明.md` | 运维摘要 |

---

## 3. HTTP 路由一览

### 3.1 相机上报（相机 → 平台）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/camera/heatbeat` | **文档正式路径**（heatbeat 为厂商原文拼写） |
| POST | `/camera/heartbeat` | 拼写别名 |
| POST | `/servlet/heartbeat` | 同文档 curl 示例兼容路径 |
| GET | `/servlet/heartbeat` | 联调备用（Query 传参） |

**完整 URL 示例：**

```text
http://120.196.88.140:9998/camera/heatbeat
```

### 3.2 平台查询（浏览器 / 运维 → 平台）

| 方法 | 路径 | 鉴权 |
|------|------|------|
| GET | `/api/v1/uovision/ir/{IMEI}/heartbeat` | 无需 Key |
| GET | `/api/v1/uovision/heartbeats` | 需 `X-Api-Key` |
| GET | `/api/v1/uovision/cameras` | 无需 Key（含心跳字段） |
| GET | `/watch/ir` | 网页总览（在线/电量/信号） |

---

## 4. 接口规范（对接文档 1.4）

### 4.1 请求

```http
POST /camera/heatbeat HTTP/1.1
Host: 120.196.88.140:9998
Content-Type: application/json

{
  "cameraCode": "863386077691243",
  "latitude": "22.589183",
  "longitude": "113.924715",
  "altitude": "31.00",
  "temperature": 79,
  "signal": 5,
  "battery": 8,
  "gps": 1,
  "model": "UML8",
  "capacity": 30419,
  "freeSpace": 30393,
  "iccid": "898604851925c0009742",
  "firmware": "H0025-X690.T"
}
```

| 参数 | 必填 | 类型 | 说明 |
|------|------|------|------|
| cameraCode | **是** | String | IMEI / MAC |
| latitude | 否 | String | 纬度 WGS84 |
| longitude | 否 | String | 经度 WGS84 |
| altitude | 否 | String | 海拔 |
| temperature | 否 | Integer | 温度（华氏度） |
| signal | 否 | Integer | 信号 1–5 |
| battery | 否 | Integer | 电量 1–10 |
| gps | 否 | Integer | 0 关 / 1 开 |
| model | 否 | String | 如 UML8 |
| capacity | 否 | Integer | SD 总容量 MB |
| freeSpace | 否 | Integer | SD 剩余 MB |
| iccid | 否 | String | 物联卡 ICCID |
| firmware | 否 | String | 固件版本 |

### 4.2 成功响应

```json
{
  "code": 0,
  "message": "OK",
  "cameraCode": "863386077691243",
  "received_at": "2026-07-20T08:15:00.123456+00:00"
}
```

### 4.3 错误响应

| HTTP | 原因 |
|------|------|
| 400 | 缺少 `cameraCode` |
| 415 | Content-Type 不是 `application/json` |
| 500 | 服务器内部错误 |

---

## 5. 代码结构

### 5.1 `uovision_camera.py`（核心模块）

```
handle_heartbeat()              # 入口：解析、存储、返回 code:0
  ├── _normalize_heartbeat_payload()   # 解析 cameraCode + 13 个遥测字段
  ├── _ensure_heartbeat_loaded()       # 内存/磁盘读取
  └── _save_heartbeat()                # 写入 latest.json + history.jsonl

get_heartbeat_status(imei)      # 单台：online、telemetry、last_heartbeat
list_heartbeat_status(codes)    # 批量查询
default_heartbeat_online_sec()  # 在线超时（默认 7200 秒）
```

**磁盘路径：**

```text
data/uovision/heartbeats/{IMEI}/latest.json    # 最新一条
data/uovision/heartbeats/{IMEI}/history.jsonl  # 历史（最多 50 条）
```

**内存结构：**

```python
_heartbeat_state: dict[str, dict]  # key = IMEI，服务重启后从磁盘恢复
```

### 5.2 `cloud_server.py`（路由层）

```
POST /camera/heatbeat      ─┐
POST /camera/heartbeat      ├── uovision_heartbeat_post()
POST /servlet/heartbeat    ─┘       │
                                    ├── _parse_heartbeat_payload()
                                    └── _uovision_heartbeat_handler()
                                              └── handle_heartbeat()

GET  /api/v1/uovision/ir/{IMEI}/heartbeat  → get_heartbeat_status()
GET  /api/v1/uovision/heartbeats             → list_heartbeat_status()
GET  /watch/ir                               → 网页 JS 读 cameras API 展示在线状态
```

### 5.3 `uovision_registry.py`（列表合并）

`list_cameras_with_status()` 在每台相机记录上追加：

```python
row["online"]                  # bool
row["last_heartbeat"]          # ISO 时间
row["last_heartbeat_age_sec"]  # 距上次心跳秒数
row["heartbeat_telemetry"]     # {battery, signal, temperature, ...}
```

---

## 6. 在线判定逻辑

```python
# 环境变量 UOVISION_HEARTBEAT_ONLINE_SEC，默认 7200（2 小时）
online = (now - last_heartbeat_epoch) <= timeout
```

| 状态 | 条件 |
|------|------|
| 在线 | 超时时间内收到心跳 |
| 离线 | 曾收到心跳但已超时 |
| 未上报 | 从未收到该 IMEI 心跳 |

---

## 7. 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `UOVISION_HEARTBEAT_ONLINE_SEC` | `7200` | 多少秒内有心跳视为在线 |
| `UOVISION_DATA_DIR` | `data/uovision` | 心跳与上传文件根目录 |
| `UOVISION_CALLBACK_BASE` | `http://120.196.88.140:9998` | setServerBase 的 dataSvr |

相机 `setServerBase` 配置：

```json
{
  "dataSvr": "http://120.196.88.140:9998"
}
```

固件自动拼接路径：`/camera/heatbeat`

---

## 8. 部署步骤

### 8.1 本地上传（Windows → 服务器）

```bash
scp -P 12222 ^
  uovision_camera.py ^
  cloud_server.py ^
  uovision_registry.py ^
  红外相机对接接口清单.md ^
  红外相机心跳接口-代码总结.md ^
  UOVision红外相机对接说明.md ^
  root@120.196.88.140:/root/elephant_cloud/
```

### 8.2 服务器重启

```bash
ssh -p 12222 root@120.196.88.140
cd ~/elephant_cloud
source uovision_config.example.sh   # 如有
bash start_cloud_server_linux.sh
# 或
pkill -f cloud_server.py; nohup python cloud_server.py --host 0.0.0.0 --port 9998 &
```

### 8.3 验证

```bash
# 1. 服务健康
curl http://127.0.0.1:9998/health

# 2. 模拟 01 号心跳
curl -X POST http://127.0.0.1:9998/camera/heatbeat \
  -H "Content-Type: application/json" \
  -d '{"cameraCode":"863386077691243","signal":5,"battery":8,"temperature":79,"model":"UML8"}'

# 3. 查询在线状态
curl http://127.0.0.1:9998/api/v1/uovision/ir/863386077691243/heartbeat

# 4. 浏览器
# http://120.196.88.140:9998/watch/ir
```

---

## 9. 五台相机 IMEI

| 编号 | IMEI（cameraCode） | 观看页 |
|------|-------------------|--------|
| 01 | 863386077691243 | /watch/ir-863386077691243 |
| 02 | 863386077682150 | /watch/ir-863386077682150 |
| 03 | 863386077672052 | /watch/ir-863386077672052 |
| 04 | 863386077686185 | /watch/ir-863386077686185 |
| 05 | 863386077682325 | /watch/ir-863386077682325 |

---

## 10. 与现有接口关系

```text
[UML8 相机 4G]
    │
    ├─ 定时 ──POST /camera/heatbeat──────────► 在线状态 /watch/ir
    │
    └─ 触发 ──POST /servlet/original2 ───────► GPU 识别 → /watch/ir-{IMEI}

配置/注册仍走：
    运维 → /api/v1/uovision/* → open.uovcloud.com:98
```

---

## 11. 关键代码片段索引

| 功能 | 文件 | 函数/路由 |
|------|------|-----------|
| 心跳处理 | `uovision_camera.py` | `handle_heartbeat()` |
| JSON 字段解析 | `uovision_camera.py` | `_normalize_heartbeat_payload()` |
| 在线查询 | `uovision_camera.py` | `get_heartbeat_status()` |
| POST 路由 | `cloud_server.py` | `@app.post("/camera/heatbeat")` |
| 查询 API | `cloud_server.py` | `uovision_ir_heartbeat()` |
| 列表合并 | `uovision_registry.py` | `list_cameras_with_status()` |
| 网页展示 | `cloud_server.py` | `watch_ir_index()` 内 JS |

---

## 12. 注意事项

1. **路径拼写**：文档正文为 `/camera/heatbeat`（非 heartbeat），三个 POST 路径均已注册。
2. **温度单位**：文档为华氏度（°F），网页总览按 °F 显示。
3. **电量/信号**：文档为 1–10 / 1–5 档位，非百分比。
4. **服务重启**：内存索引清空，但 `latest.json` 会自动加载，在线状态不丢失。
5. **防火墙**：公网 TCP **9998** 须对相机 4G 出口可访问。

---

**维护：** 大象识别项目组  
**主程序：** `cloud_server.py`（端口 9998）
